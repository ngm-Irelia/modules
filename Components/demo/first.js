document.addEventListener("DOMContentLoaded", function(event) {
  //这里是使用方式
  //let lc = new Component.LoadComponent();
  //lc.run();            // 加载所有组件
  //lc.run(["barChart"]);  // 指定加载部分组件
  //Component.loadComponent.run();
});